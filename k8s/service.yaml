apiVersion: v1
kind: Service
metadata:
  name: devsecops-app
  namespace: devsecops
  labels:
    app: devsecops-app
spec:
  selector:
    app: devsecops-app
  ports:
    - port: 80
      targetPort: 5000
  type: ClusterIP
